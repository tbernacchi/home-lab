apiVersion: v1
kind: LimitRange
metadata:
  name: default-limits
  namespace: argo-rollouts
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 25m
      memory: 64Mi
    default:
      cpu: 200m
      memory: 128Mi
